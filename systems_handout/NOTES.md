The sender immediately encodes each 160-byte frame plus a two-byte sequence check into 32 independently delivered 10-byte fragments using a Vandermonde erasure code over GF(256).  Any 18 distinct fragments reconstruct the frame, so recovery does not wait for a later source frame or a feedback round trip.  The one-byte wire header contains a three-bit sequence tag and five-bit fragment index, and the receiver combines that tag with `T0` and `DELAY_MS` to identify the unique sequence whose deadline has not passed.  The receiver validates the decoded sequence check and forwards each recovered frame immediately while keeping all state bounded.  Thirty-two 10-byte fragments per 160-byte source frame produce exactly 2.0x relay overhead with no feedback traffic.  Grade this submission at `--delay_ms 70`; profile A seed 1 had no misses, and profile B seeds 1–5 had 0.00%–0.33% misses.  Although 68 ms passed the visible seeds, its worst miss rate was 0.87%, while 65 ms failed at 3.07%, so 70 ms retains margin for hidden profiles.  Long loss bursts that erase at least 15 fragments or hidden delay distributions substantially beyond the selected deadline can still break recovery.
